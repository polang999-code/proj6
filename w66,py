scores = [88, 92, 75, 100, 64]
print(scores[1:3])  # [92, 75] → starts at index 1, stops before 3
print(scores[:2])   # [88, 92] → start from beginning
print(scores[3:])   # [100, 64] → from index 3 to end
